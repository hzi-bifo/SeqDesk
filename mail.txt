Hi — please implement the manifest-driven pipeline contract and generic integration we discussed. I’ve added the validation scaffolding you’ll need:

- Manifest schema (zod): `src/lib/pipelines/manifest-schema.ts`
- Package validator script: `scripts/validate-pipeline-package.ts`
  Run with: `npm run pipeline:validate`
- Author checklist: `docs/PIPELINE_PACKAGE_CHECKLIST.md`

Implementation checklist (please follow in this order):
1) Package loader validation
   Use `ManifestSchema` and add cross-file validation (folder name == package.id, definition.pipeline, registry.id, file existence).
   Fail fast on errors; warn on drift.

2) Generic adapter + parser runtime
   Implement a parser runner for `parsers/*.yaml` and generic output discovery driven by `manifest.outputs`.
   Use `parsed.from` + `parsed.map` for metadata.

3) Generic executor
   Build a pipeline-agnostic executor that uses `manifest.execution.pipeline/version/profiles/defaultParams` and add `paramMap` + `paramRules` support for config→flags.
   Update the start route to use this instead of `prepareMagRun`.

4) Docs alignment
   Update `PIPELINE_STORE.md` and `docs/PIPELINE_PACKAGE_DESIGN.md` to reflect manifest as source of truth for execution + IO.

5) Template package
   Add `pipelines/_example/` with a minimal working manifest/definition/registry/samplesheet and make the loader ignore folders starting with `_`.

If you need to see the exact contract and validation expectations, check `docs/PIPELINE_PACKAGE_CHECKLIST.md` and `src/lib/pipelines/manifest-schema.ts`.
