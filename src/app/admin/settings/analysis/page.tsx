"use client";

import { AnalysisEnvironmentSettings } from "@/components/settings/AnalysisEnvironmentSettings";

/** The same controls inside administration; the original Explore URL remains valid. */
export default function ReportAnalysisSettingsPage() {
  return <AnalysisEnvironmentSettings administration />;
}
